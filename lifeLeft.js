function timeLeft(age) {
    var yearsRemaining = 90 - age;
    var monthsRemaining = (90 * 12) - (age * 12);
    var weeksRemaining = (90 * 52) - (age * 52);
    var daysRemaining = (90 * 365) - (age * 365);
}

timeLeft(22);